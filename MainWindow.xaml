<!--
    Main screen for the simplified quote import flow. The code-behind wires these controls
    to the current import workflow while the ViewModel remains available for later binding work.
-->
<Window x:Class="QuoteImporter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quote Importer"
        Height="600"
        Width="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,12">
            <!-- Buttons start the two user-visible actions for the import demo. -->
            <Button Content="Load Quote" Width="120" Height="32" Margin="0,0,8,0" Click="LoadQuote_Click" />
            <Button Content="Import Quote" Width="120" Height="32" Margin="0,0,16,0" Click="ImportQuote_Click" />
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="8,0,4,0" />
            <TextBlock x:Name="SummaryText" Text="Ready" VerticalAlignment="Center" />
        </StackPanel>

        <!-- Explicit columns keep the review grid focused on the fields needed for validation. -->
        <DataGrid x:Name="QuoteGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" />
                <DataGridTextColumn Header="Molport ID" Binding="{Binding MolportId}" />
                <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" />
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPriceUsd}" />
                <DataGridTextColumn Header="Net Price" Binding="{Binding NetPriceUsd}" />
                <DataGridTextColumn Header="Errors" Binding="{Binding ErrorText}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
